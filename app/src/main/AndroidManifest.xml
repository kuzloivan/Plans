<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chisw.com.plans">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <application
        android:name=".core.PApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.activities.SplashActivity"
            android:label="@string/app_name">
            <!--android:theme="@style/AppTheme.Fullscreen">-->

        </activity>

        <activity
            android:name=".ui.activities.LogInActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.MediaActivity"
            android:label="@string/title_activity_media"></activity>

        <activity
            android:name=".ui.activities.PlannerActivity"
            android:label="@string/title_activity_planner"
            android:theme="@style/AppTheme.PlannerTheme"></activity>

        <activity
            android:name=".ui.activities.AlarmActivity"
            android:label="@string/title_activity_alarm"
            android:theme="@style/AppTheme.PlannerTheme" />

        <activity
            android:name=".ui.activities.SettingsActivity"
            android:label="@string/title_activity_settings" />

        <receiver android:name=".core.Receivers.Receiver" />

        <receiver
            android:name=".core.Receivers.RebootReceiver"
            android:enabled="true" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
